# Scripts

## Overview

Scripts for orchestrating the frontend and backend spin-up.

## Usage

```bash
source 0_setup.sh
```

This will:
1. Create a virtual environment if not present
2. Install Python dependencies from requirements.txt